Flask==3.0.3
gunicorn==22.0.0

# Data + PDF parsing
pandas==2.2.2
pdfplumber==0.11.4

# AI (optional, safe if no key provided)
openai>=1.40.0

# Google auth + Gmail API
google-auth==2.31.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.141.0
google-auth-httplib2==0.2.0   # ✅ needed by googleapiclient
httplib2==0.22.0              # ✅ also needed for Gmail API
openpyxl==3.1.5   # for .xlsx
xlrd==2.0.1   # for .xls

Flask
requests


# Email handling (standard library covers most, but smtplib not required here)
